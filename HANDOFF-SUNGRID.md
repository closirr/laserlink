# HANDOFF: SUNGRID — ізометричний ремейк механік Harvest: Massive Encounter

> **Це головний документ для нової сесії.** Прочитай його ЦИЛІКОМ перед будь-яким кодом.
> Попередній проєкт (еталонний референс): `C:\Users\closirr\.zcode\workspace\default\laserlink\`
> (repo: https://github.com/closirr/laserlink, живе демо: https://closirr.github.io/laserlink/).

## 0. ЗАВДАННЯ (від користувача, дослівно по суті)

Зробити НОВУ повну веб-гра — точну копію МЕХАНІК Harvest: Massive Encounter — в ІЗОМЕТРИЧНОМУ
стилі, використовуючи напрацювання laserlink. Публікація: **тільки GitHub + GitHub Pages**
(closirr.github.io/sungrid). НЕ публікувати на itch.io / CrazyGames / Poki — цього разу платформи
НЕ потрібні. Повна гра: багато рівнів, меню, продуманий баланс, тести.

Обов'язкові механіки (вимоги користувача, пріоритет №1):
1. **Лазери стріляють один в одного** — приймаючий лазер стає потужнішим і б'є далі (USP).
2. **Передача енергії через «атоми сонця»** — видимые кульки енергії течуть лініями мережі.
3. **Вироблення енергії всередині бази** (power plants + ядро).
4. **Перенавантаження мережі (overload)**: якщо занадто мало лінків (не вистачає пропускної
   здатності) або занадто багато виробляється енергії → мережа перевантажується.
5. **Витрати енергії**: будівлі споживають; енергія керує швидкістю (будівництво/перезарядка/ремонт).

Структура (затверджено): **гібрид** — кампанія ~12 карт + режим Wave (10 хвиль на час) + Endless.
Репозиторій: **новий** `closirr/sungrid` + свій GitHub Pages.

Юридично: механіки не копіруються копірайтом — реалізуємо власний код і арт, назва і стиль
оригінальні (SUNGRID). Це стандартна практика ремейків (як OpenTTD).

## 1. ДОСЛІДЖЕННЯ: ЯК ПРАЦЮЄ СПРАВЖНІЙ HARVEST (консолідовано з джерел)

Джерела: JayIsGames review/walkthrough, IGN review (2008), IndieFAQs, Codex Gamicus,
Casual Game Guides, Reddit r/oxeyegames, офіційний oxeyegames.com.

### Два ресурси
- **Кредити** — валюта для будівництва. Добуваються **mineral harvesters** з мінеральних
  родовищ на карті ("mine hills for minerals → convert to cash").
- **Енергія** — НЕ валюта, а ПОТІК. Генерується **power plants**. Енергієм не платиш за будівлі;
  будівлі споживають її постійно щоб ПРАЦЮВАТИ.

### Енергомережа (ядро гри)
- Енергія рухається від plants до споживачів, **«стрибаючи» між energy links** (вейпоінтами),
  які гравець ставить сам. Сітка лінків = «glowing web».
- Фізично виглядає як **кульки-пелети**, що течуть лініями (гравець називає їх «атоми сонця»).
- Будівлі можна ставити лише в радіусі лінка/рослини (як пілони в laserlink).
- **Енергія впливає на 3 речі** (цитата JayIsGames): "The more energy a building gets, the
  faster it's erected, the faster it reloads, and the faster it repairs itself".
  → Енергія = ШВИДКІСТЬ. Брак енергії = повільна оборона, а не повна зупинка.
- **Перенавантаження**: лінки можуть ставати "overcharged" (звуковий сигнал, небезпека) коли
  виробництво/потік перевищує можливості мережі. У нашій версії (конкретизація користувача):
  мало лінків або надлишок виробництва → overload.
- **Блекаут від ворога**: є ворог **energy sapper**, який висмоктує ВСЮ енергію з лінка —
  все downstream позбавляється живлення (Casual Game Guides, зірка-5 загроза).

### Будівлі оригіналу (~6 типів, кожна з апгрейдами/режимами)
| Будівля | Функція |
|---|---|
| Power Plant | генерує енергію |
| Energy Link (waypoint) | транспорт енергії + радіус будівництва |
| Mineral Harvester | добуває кредити з родовищ; сама споживає енергію |
| Laser Tower | основна оборона; ЛІНКУЄТЬСЯ в інші лазери |
| Missile Turret | площинна/ShVIDKA оборона, споживає більше енергії |
| Bomb | ставиться, заряджається енергією, потужний вибух (over-charged bombs) |

### Лазер-лінки (USP — головне!)
- Будь-який Laser Tower може **спрямувати промінь в інший Laser Tower**: приймаючий отримує
  приріст шкоди І дальності. Ланцюги довільної глибини.
- Reddit-стратегія: "link up 5–10 to make a powerful laser, but keep the front 1–2 rows
  unlinked" — передні ряди окремі (по одній цілі кожен), задні злиті в death-laser.
- Ланцюг **потребує часу на фокусування** повної сили (ramp-up) — не миттєвий буст.
- Трейдофф: 3 окремі лазери вражають 3 цілі одночасно; ланцюг — одну, але сильно.
- **Камікадзе-дрони** спеціально карають переланцюгованість (летять у базу, коли все злито).
- **Teleporter ships** висять ПОЗА дальністю одиночних веж — їх достають лише подовжені
  ланцюгами промені. Тримай їх високими в пріоритеті цілей.
- QoL, якого просили гравці: "unlink all" хоткей — додай (Shift+клик по ланцюгу).

### Вороги (10 в оригіналі; ключові поведінки)
- UFO / механо-боти / сотні-тисячі за хвилю (swarm).
- **Kamikaze drones** — пріоритетно летять у лазерні скупчення/будівлі.
- **Teleporter ships** — hover поза дальністю, телепорт-стрибки.
- **Energy sapper** — сідає на лінк і п'є всю енергію через нього.
- **Tiny rockets** — дрібні швидкі, з'являються на threat ~17, потрібен point-defense.
- Загрози ескалюють "threat levels" (не фіксовані хвилі в Normal-режимі).

### Режими оригіналу (3 планети: Hephaestus, Aries, Poseidon)
- **Normal**: виживання з ескалацією, максимізуй кредити/threat-рівень.
- **Wave**: 10 дедалі більших хвиль, ціль — зачистити якнайшвидше.
- **Insane**: старт глибоко на ворожій території з бонусними ресурсами.
- **Rush**: багаті ресурси; завдати 50 000 шкоди якнайшвидше, обороняючись.
- **Creative**: sandbox, повний контроль над хвилями.
- Поразка = знищення бази. Онлайн-рекорди (нам не потрібно).

## 2. ДИЗАЙН SUNGRID (правила гри; стартові числа — тюнювати ботом)

### Поле
- Сітка **28×28** тайлів, iso-проєкція **2:1** (tile 64×32 px), canvas 1280×720,
  масштаб під вікно як у laserlink (`fitCanvas`).
- Тайли: `.` земля, `#` скеля (блокує будівництво і прохід), `M` мінеральне родовище
  (багате `W`), `S` спавн ворогів, `K` ядро бази (старт: 2 plants + 3 links навколо,
  як в оригіналі — "you start with two power generators and three power links").

### Ресурси
- **Кредити**: старт 200. Harvester +8 кр/с на родовищі (`W`: +14). Продаж будівлі 70%.
  Вбивство ворога: +2..+40 кр. Бонус за хвилю в Wave-режимі.
- **Енергія** (потік, не гроші):
  - Plant +10 е/с (T2 +16, T3 +24).
  - Лінк передає до **20 е/с**, будівельний радіус 2.5 тайли (T2 3.2, T3 4).
  - Споживання (idle → активний): laser 2→6 е/с, missile 3→10, harvester 4, bomb заряд 40,
    link 0.5.
  - Ядро: невеликий базовий генератор +4 е/с і радіус 3.5 (щоб старт був живий).

### Енергопостачання як ШВИДКІСТЬ (замість on/off)
- Кожен споживач має `supplyRatio` 0..1 = отримано/запит за останню секунду.
- Ефекти: reload time / supplyRatio; repair speed × supplyRatio; будівництво завершується
  швидше при високому supplyRatio (будівля будується 4с базово, до 2с при повному).
- Тобто brownout — це в'язкість, а не смерть (як у Harvest). Повний 0 (лінк знищено) = offline.

### Overload (вимога користувача №4 — ОБОВ'ЯЗКОВО)
- Потік через лінк > його ємності → `heat += перевищення × k` (кожен тік).
  heat 0..100; охолодження, коли потік в нормі.
- heat > 60: кульки в лінку червоніють + дзвін overcharge (як "overcharged" звук оригіналу).
- heat = 100: **лінк вигорає** — вибух, АОЕ-шкода, лінк зникає → всі будівлі за ним
  позбавляються мережі (треба терміново перебудувати). Це і є "перенавантаження мережі".
- Гравець бачить heat-бар на лінку при виборі.

### Розподіл енергії (модель потоку — просто і зрозуміло)
- Кожен тік (0.1с game-tick): джерела пушать енергію в сусідні лінки (радіус з'єднання 2.5),
  лінки транспортують далі до 20 е/с на лінк, споживачі тягнуть що є.
- Реалізація: мережа = граф; BFS від джерел; на кожному кроці енергія капиться пропускною
  здатністю лінків. Cultки-«атоми» — візуалізація потоку по ребрах (кількість кульок ∝ потоку).

### Лазер-ланцюги (вимога №1 — ГОЛОВНЕ)
- Laser Tower: базово 25 DPS, дальність 3.0 тайли, енергія 6 е/с при стрільбі.
- **Feeder-режим**: клік по лазеру → кнопка/хоткей LINK → клік по іншому лазеру в радіусі 3.5.
  Фідер перестає стріляти сам і вливає промінь у приймач.
- Кожен feeder: приймачу +50% DPS і +25% дальності (множаться: 2 фідери ×2.25 DPS,
  5 фідерів ×11.4 — «death laser», як в оригіналі 5–10 у ланцюг).
- **Ramp-up**: після лінку потужність росте 0→100% за 2с (фокусування, як в оригіналі).
- Розлінк: кнопка UNLINK на панелі + Shift+клік по фідеру. "Unlink all" — хоткей U.
- Приймач з ≥2 фідерами: промінь товщий, біліше ядро, звук потужніший (візуальний payoff).

### Решта будівель
- Missile Turret: 40 шкоди, АОЕ 1.2, темп 1.8с, дальність 3.5, не може бути фідером.
- Bomb: 30 кр, заряджається 40 е (5с при повному supply), вибух 300 шкоди АОЕ 2.5.
  Детонує вручну (клік) або від контакту ворога.
- Всі будівлі: HP, саморемонт × supplyRatio, 3 тіри апгрейдів (кредитами).

### Вороги (8 типів)
| Тип | HP | Швид. | Поведінка |
|---|---|---|---|
| crawler | 40 | 1.5 | базовий, йде до ядра, гризе будівлі на шляху |
| swarm | 10 | 2.2 | пачки по 5-8 |
| tank | 300 | 0.8 | повільний, багато HP |
| kamikaze | 20 | 3.2 | цілить у НАЙБІЛЬШИЙ кластер лазерів/лінків, вибух 60 АОЕ 1.5 |
| teleporter | 150 | 1.0 | hover на межі дальності, телепорт-стрибки ближче; вразливий для подовжених ланцюгів |
| sapper | 60 | 1.3 | сідає на найближчий лінк, п'є 15 е/с з нього (downstream блекаут) поки не вбити |
| rocket | 15 | 4.5 | швидка дрібна, пізні threat (point-defense) |
| boss | 3000 | 0.6 | фінал кампанії / кожні 10 хвиль Endless, АОЕ-удари по будівлях |

Threat-ескелація: бюджет хвилі = 10 × threat^1.2, threat +1 кожні 35с (Normal/Endless);
Wave-режим: 10 фіксованих хвиль. (Формула з `wavePoints` laserlink як відправна точка.)

### Режими SUNGRID
- **Кампанія 12 карт** (прогресія, зірки 1-3, розблокування):
  L1 мережа+plants, L2 харвестери+кредити, L3 перший лазер-лінк (туторіали з тостами),
  L4-8 standard (1-2 спавни), L9-11 hard (sappers, teleporters, kamікадзе), L12 бос.
- **Wave**: карта-арена, 10 хвиль на час, медаль (золото/срібло/бронза за час).
- **Endless**: threat без ліміту, рекорд хвиль у localStorage.
- Пауза/2×/fullscreen/звук — все як у laserlink.

### Ізометричний рендер
- `sx = (c - r) * 32 + originX; sy = (c + r) * 16 - h * 24 + originY` (h у "поверхах").
- Малюємо тайли-ромби, потім об'єкти **painter's algorithm**: сортування по (c + r + h).
- Будівлі — екструдовані призми з світними верхами (неон-палітра laserlink: фон #070b16,
  cyan #4de1ff, magenta #ff5cf0, gold #ffd94d, зелений #7dff9a).
- Енергокульки — `drawPrismBeams` flowing dots з laserlink, адаптовані під iso-ребра;
  колір: gold → помаранч → червоний по heat лінка.
- Пікінг: обернений iso-перетворення (див. прототип формули в розділі 5).

## 3. РЕФЕРЕНСИ НА КОД LASERLINK (що звідки брати)

Розташування: `C:\Users\closirr\.zcode\workspace\default\laserlink\`. Репо клонувати НЕ треба —
код локально. AGENTS.md вимагає CodeGraph для навігації — laserlink НЕ має `.codegraph/`;
або ініціалізуй (`codegraph init`), або читай файли напряму (вони невеликі).

| Потрібна фіча | Файл:символ | Що робити в sungrid |
|---|---|---|
| Каркас, стани, цикл | `js/main.js`: `App`, `frame()`, `window.advanceTime`, `render_game_to_text`, deep-link `?level=` | скопіювати каркас, замінити Game |
| Мережа будівництва (пілони) | `js/game.js`: `recomputeNetwork()`, `inNetwork()`, `netNodes` | основа для енерголінків (радіуси → з'єднання-граф) |
| Ланцюги підсилення (ПРІОРИТЕТ) | `js/game.js`: `recomputeLinks()`, `linkPrism()`, `autoLink()`, `Tower.boost` | замінити prism→laser: фідер=laser, приймач=laser; додати ramp-up і multiplier-формулу з розділу 2 |
| Економіка | `js/game.js`: `income()`, extractor-гілка `updateTowers()`, `place()/sell()/upgrade()` | розділити на кредити (harvester) і потік енергії (plant) |
| Хвилі | `js/game.js`: `buildComposition()`, `callWave()`, `updateWaves()`, `wavePoints` в data.js | узяти структуру, додати threat-ескелацію і спец-таргетинг |
| Вороги | `js/entities.js`: `Enemy.update()` (flow-стерінг), `moveStep()` (ОСЬОВА КОЛІЗІЯ — критично!), chewing при fd=-1, `flash` | брати як є + нові поведінки (kamikaze/sapper/teleporter таргетинг) |
| Пошук шляху | `js/flowfield.js`: `FlowField` (BFS, no-corner-cut) | брати ЯК Є |
| Дані/карти | `js/data.js`: `CFG`, `TOWERS`, `ENEMIES`, `LEVELS` (стрічкові карти 24×14) | формат лишити, карти нові 28×28 |
| Промені | `js/render.js`: `drawLaserBeams()` (3-шаровий glow-промінь), `drawLaserBeams` pierce-гілка | стиль променів фідер→приймач і приймач→ціль |
| Кульки, що течуть | `js/render.js`: `drawPrismBeams()` (flowing dots по лінії з phase) | ЕНЕРГОКУЛЬКИ по лінках (вимога №2) |
| Частинки/тряска | `js/render.js`: `drawParticles`, `spawnBurst`, `game.shake` | як є |
| Меню/HUD/ввід | `js/ui.js`: екрани, `buildPalette`, `refreshTowerPanel`, LINK-флоу (`tp-link`), `canvasPos()` | СКРИТИЧНЕ: `canvasPos()` перерахувати під iso (розділ 5) |
| Звук | `js/audio.js`: процедурний WebAudio, `Snd.laser(intensity)` гул | додати "overcharge" дзвін при heat>60 |
| Збереження | `js/save.js` | змінити KEY на sungrid-save-v1 |
| Тестові хуки | всюди: `render_game_to_text`, `advanceTime`, `?test=1` | обов'язково зберегти (нужні для Playwright-скіла) |
| Баланс-бот | `js/bot.js` + `tools/balance.js` (vm-стенд, watchdog stuck, seeded `tools/trace.js`) | адаптувати стратегію: plants→links→harvesters→lasers→chain |
| Функц. тести | `tools/functest.js` (реальні кліки палітра→канвас, 17 тестів) | адаптувати (координати iso!) |
| Скріни UI | `tools/shots.js` | як є, нові id екранів |
| Валідатор карт | `tools/validate.js` (BFS spawn→core, довжини рядків) | адаптувати 28×28 + родовища |
| Локальний сервер | `tools/server.js` (порт 8123) | скопіювати |
| Playwright | `tools/pgclient.mjs` (копія клієнта скіла в проєкт) + локальний `playwright@1.60.0` у node_modules (вже встановлено в laserlink — можна npm i playwright@1.60.0) | скопіювати підхід |

## 4. ЛЕССОНИ LASERLINK (НЕ ПОВТОРЮВАТИ БАГИ)

1. **Глобальний `CELL`** — оголоси константи проєктуantly (const TILE тощо) ДО використання.
2. **Порядок `<script>`** — бібліотечні модулі ДО main.js, який на них посилається.
3. **Вороги зрізали кути крізь стіни/скелі** → залазили всередину тайла і зависали назавжди.
   Фікс: axis-separated колізія `moveStep()` + при fd=-1 (відрізаний) ворог гризе найближчі
   будівлі (у sungrid: kamikaze сам так цілиться — механіка з багу стає фічею).
4. **Будівля на клітинці з ворогом** → ворог «звільнявся» і проходив крізь базу. `canPlace()`
   зобов'язаний перевіряти ворогів на тайлі.
5. **Bot-лівелоки**: не перебудовувати, поки вороги гризуть будівлі; перевіряти після кожної
   постановки, що спавн не запечатаний (інакше sell і назад).
6. **Баланс через бота**: 3+ репи на карту, watchdog "stuck" (90с без прогресу = фейл),
   seeded Math.random для відтворення (tools/trace.js — зразок).
7. zip не потрібен цього разу (тільки Pages) — але якщо пакуватимеш: Windows
   Compress-Arogate робить backslash-шляхи → 404. (7z ок.)
8. GitHub Pages: після увімкнення Pages перший білд ~1хв; 404 = ще не збудувався або
   не запушено.

## 5. ISO-МАТЕМАТИКА (готові формули, щоб не гадати)

```js
const TILE_W = 64, TILE_H = 32;                 // 2:1
// grid (c,r) → screen (центр тайла), origin — центр canvas
function isoProject(c, r, h = 0) {
  return {
    x: (c - r) * (TILE_W / 2) + ORIGIN_X,
    y: (c + r) * (TILE_H / 2) + ORIGIN_Y - h * 24, // h — "поверхи" будівлі
  };
}
// screen → grid (пікінг): обернене перетворення без h (будівлі пікаємо по основі)
function isoPick(sx, sy) {
  const dx = sx - ORIGIN_X, dy = sy - ORIGIN_Y;
  const c = (dx / (TILE_W / 2) + dy / (TILE_H / 2)) / 2;
  const r = (dy / (TILE_H / 2) - dx / (TILE_W / 2)) / 2;
  return { c: Math.floor(c), r: Math.floor(r) };
}
// сортування рендера об'єктів (painter's algorithm):
entities.sort((a, b) => (a.c + a.r + (a.h || 0)) - (b.c + b.r + (b.h || 0)));
// ромб тайла (для малювання):
function diamondPath(ctx, cx, cy, w = TILE_W, h = TILE_H) {
  ctx.beginPath();
  ctx.moveTo(cx, cy - h / 2); ctx.lineTo(cx + w / 2, cy);
  ctx.lineTo(cx, cy + h / 2); ctx.lineTo(cx - w / 2, cy); ctx.closePath();
}
```
КАМЕРУ робити фіксованою (вся карта 28×28 вміщується: ширина 28×64=1792px — НЕ вміщується в
1280! → або карта 20×20, або легкий zoom/pan колесом+краями. РЕКОМЕНДАЦІЯ: карта **20×20**
(1280×640 iso-ромб + відступи) — тоді без камери, простіше пікінг і тести. Якщо користувач
захоче 28 — додати камеру з клампом меж).

## 6. ФАЗИ ВИКОНАННЯ (новій сесії; комітити після кожної фази)

Позначки `[A]`/`[B]` = можна віддати сабагенту (див. розділ 7 — стратегія і шаблони промптів).

1. **Init**: `gh repo create sungrid --public` у `C:\Users\closirr\.zcode\workspace\default\sungrid`;
   скелет з laserlink (index.html, css, js/*, tools/server.js, tools/pgclient.mjs); прогін
   `node tools/server.js` → перевірка в браузері через Playwright-скрипт скіла.
   ГОЛОВНА СЕСІЯ САМА (фундамент, все залежить від нього). КОМІТ.
2. **Iso-рушій**: проєкція/пікінг/painter-sort, тайли-ромби (земля/скеля/родовища),
   тест-карта, скріншот-перевірка (клік-тайл підсвічується). ГОЛОВНА СЕСІЯ САМА. КОМІТ.
   ↳ ПАРАЛЕЛЬНО ЗАПУСТИТИ [A]: кампанійні карти + валідатор (незалежна робота з даними).
3. **Енергомережа**: plants/links граф, потік (0.1с тік), кульки-«атоми» по ребрах,
   supplyRatio, heat/overload/вигоряння лінка. ГОЛОВНА СЕСІЯ. ПАРЛЕЛЬНО [A] ще працює,
   за потреби [B]: звук+ефекти (overcharge-дзвін, вибух лінка). КОМІТ.
4. **Будівлі і лазери**: 6 будівель, laser→laser LINK-флоу, ramp-up, UNLINK, бомба.
   ГОЛОВНА СЕСІЯ (найтонша механіка — не віддавати). КОМІТ.
   ↳ [A] на цей момент вже злити карти головною сесією (review + інтеграція).
5. **Вороги**: FlowField, 8 типів, таргетинг kamikaze/sapper/teleporter, threat-хвилі.
   ГОЛОВНА СЕСІЯ. ПАРАЛЕЛЬНО [B]: бот + баланс-стенд (tools/balance.js адаптація). КОМІТ.
6. **Структура**: меню-екрани, зірки, збереження, Wave/Endless, туторіальні тости.
   ГОЛОВНА СЕСІЯ інтегрує; [A]: progres.md/README + метадані режимів. КОМІТ.
7. **Баланс**: прогін бота (він готовий від [B]), тюнинг констант до цілей
   (L1-8 ~100%, L9-12 50-80%, Endless 12+). ГОЛОВНА СЕСІЯ + [B] ітерацією. КОМІТ.
8. **Тести**: [A]: tools/functest.js адаптація (реальні кліки: будівля, LINK лазер→лазер,
   буст DPS у state-JSON, overload, sapper-блекаут); [B]: tools/shots.js + візуальний
   фікс дрібних косяків. ГОЛОВНА: зливає, проганяє, чинить регресії. КОМІТ.
9. **Pages**: gh api pages POST, push, перевірка closirr.github.io/sungrid 200 + гра в
   iframe. README + progress.md. ФІНАЛЬНИЙ КОМІТ + ПУШ.

## 7. САБАГЕНТИ: СТРАТЕГІЯ РОЗПАРАЛЕЛЮВАННЯ (ОБОВ'ЯЗКОВО ДО ВИКОРИСТОВУВАТИ)

### Жорсткі правила
1. **Максимум 2 сабагенти одночасно** (вимога користувача, порушувати заборонено).
   Третій — тільки після завершення одного з двох.
2. **Розбивай таски на сабагентів автоматично** — це прискорює роботу і вимога користувача.
   Дивись мапу паралелізації нижче; відхиляйся лише коли фаза справді послідовна.
3. **Промпт сабагента — САМОДОСТАТНІЙ**: сабагент стартує з чистого контексту. Кожен промпт
   мусить містити: (а) шлях до цього HANDOFF і наказ прочитати потрібні розділи; (б) точний
   список файлів, які він читає (референси laserlink) і які СТВОРЮЄ/ПРАВЕТЬ (у sungrid);
   (в) обмеження (vanilla JS, без білд-кроку, збереження тестових хуків); (г) очікуваний
   результат (файли + коротке summary).
4. **Власність файлів**: сабагенти НЕ чіпають файли головної сесії і один одного
   (конфлікти на диску). Розподіл за фазами — у розділі 6. `js/main.js`, `js/game.js`,
   `js/entities.js`, `js/render.js` під час активної роботи — тільки головна сесія.
5. **Коміти — тільки головна сесія**: сабагент повертає зміни на диску + summary, головна
   сесія дивиться `git diff`, інтегрує, комітить. Так історія чиста, і ревʼю не губиться.
6. Типи: `general-purpose` — писання/адаптація коду, прогони тулз; `Explore` — тільки читання
   (розвідка laserlink, пошук референсів) — дешевший для read-only задач.

### Як запускати (механіка)
- Обидва сабагенти — ОДНИМ повідомленням з двома викликами Agent tool (тоді вони йдуть
  паралельно), параметр `subagent_type`, за потреби `run_in_background: true`.
- Коли сабагент повернувся: перевір артефакти (файли існують, тулзи проганяються), потім
  інтеграція. Якщо результат поганий — один повторний запуск з уточненим промптом, не більше.
- Для фонових сабагентів: перевіряй результат через TaskOutput перед інтеграцією.

### Мапа паралелізації (деталізація розділу 6)
| Фаза | Головна сесія | Сабагент A | Сабагент B |
|---|---|---|---|
| 1-2 | скелет + iso-рушій | карти + валідатор (`js/maps.js`, `tools/validate.js`) | — |
| 3 | енергомережа/потік | (продовжує карти/туторіальні тексти) | звук+ефекти (`js/audio.js`) |
| 4 | будівлі + лазер-лінки | фініш карт → інтеграція | — |
| 5 | вороги + хвилі | README/progress | бот + `tools/balance.js` |
| 6 | меню/режими/збереження | метадані режимів, тексти UI | — |
| 7 | тюнинг констант | — | ітерації бота по фідбеку |
| 8 | зливає + чинить регресії | `tools/functest.js` | `tools/shots.js` + візуальні фікси |
| 9 | Pages + README | — | — |

### Готовий шаблон промпту сабагента (копіюй і прав під таск)
```
Ти працюєш над грою SUNGRID (ізометричний ремейк механік Harvest: Massive Encounter).
ОБОВ'ЯЗКОВО спочатку прочитай: C:\Users\closirr\.zcode\workspace\default\laserlink\HANDOFF-SUNGRID.md
(розділи 0, 2 і < потрібні >), потім файли-референси: < шляхи в laserlink >.

Твоя задача: < конкретний список deliverables >.
Створюй/прав ЛИШЕ ці файли в C:\Users\closirr\.zcode\workspace\default\sungrid\: < шляхи >.
Обмеження: vanilla JS без білд-кроку; зберігай window.advanceTime/render_game_to_text;
не коміть git; не чіпай інші файли.
Перевір себе: < як прогнати (node tools/... або браузерний тест) >.
Поверни: список створених файлів + коротке summary рішень (3-5 рядків).
```

### Приклад готового промпту (фаза 2, сабагент A — карти)
```
Ти працюєш над грою SUNGRID. Спочатку прочитай C:\Users\closirr\.zcode\workspace\default\laserlink\HANDOFF-SUNGRID.md
(розділи 0, 2 «Поле», 5 «ISO-МАТЕМАТИКА» — карта 20×20), і як зразок формату карт:
C:\Users\closirr\.zcode\workspace\default\laserlink\js\data.js (LEVELS) та
C:\Users\closirr\.zcode\workspace\default\laserlink\tools\validate.js.

Задача: створи js/maps.js у C:\Users\closirr\.zcode\workspace\default\sungrid\ з 12 картами
кампанії (20×20, легенда . # M W S K; L1-L3 туторіальні за розділом 2; L12 бос) + картою
арени для Wave/Endless. Адаптуй tools/validate.js під 20×20 і прогони на всіх картах —
всі мають пройти (шлях S→K існує, рівні довжини рядків, ≥1 родовище біля старту).
Не чіпай інші файли, не коміть. Поверни summary по кожній карті (спавни/родовища/фішка).
```

## 8. КРИТЕРІЇ ГОТОВНОСТІ (приймання)

- [ ] Лазер→лазер лінк працює і ВИДНО підсилення (DPS/далекість у панелі, товщий промінь).
- [ ] Кульки-«атоми» течуть від plants по лінках до споживачів.
- [ ] Overload: спали лінки/надлишок production → heat росте → лінк вибухає; гравець відчуває.
- [ ] Sapper висмоктує лінк → downstream блекаут.
- [ ] Кампанія 12 карт проходиться, зірки/розблокування зберігаються (перезавантаження стор).
- [ ] Wave і Endless працюють.
- [ ] functest зелений, скріни всіх екранів ОК, 0 помилок консолі.
- [ ] GitHub Pages 200, гра грається онлайн.
- [ ] Коміти після кожної фази в repo closirr/sungrid.

## 9. WORKFLOW-НАГАДУВАННЯ (як у попередній сесії)

- Скіл `develop-web-game`: мали кроки, `window.advanceTime(ms)` + `render_game_to_text()`
  обов'язково, Playwright-клієнт після кожної зміни, скріншоти дивитись очима.
- **Сабагенти — АКТИВНО, за мапою розділу 7** (максимум 2 одночасно, вимога користувача;
  розбивай таски на них автоматично, промпти самодостатні за шаблоном, комітить тільки
  головна сесія).
- `progress.md` в sungrid: Original prompt зверху, оновлювати після кожного чанку.
- Сервер: `node tools/server.js` (порт 8123). Playwright 1.60.0 локально (`npm i playwright@1.60.0`)
  — браузери вже в кеші (chromium-1223).
- Мова комунікації з користувачем: українська.
